package com.ctrlaltelite.copshop.persistence.models;

import com.ctrlaltelite.copshop.persistence.interfaces.IDatabase;

/**
 * TODO: Interface for the listing table
 */
public class ListingModel {
    private IDatabase database;

    public ListingModel(IDatabase database) {
        this.database = database;

        // Initialize mock data
    }

    // Create new listing

    // Update listing information from object

    // Get and convert listing information to object
}
